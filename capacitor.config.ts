import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'listado-deseos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
